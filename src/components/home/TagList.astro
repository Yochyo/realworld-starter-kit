---
import type { TagDTO } from "../../api/api";
export type Props = { tags: TagDTO };
const { tags } = Astro.props;
---

<div class="tag-list">
  {
    tags.tags.map((tag) => (
      <a href={`/?tag=${tag}`} class="tag-pill tag-default">
        {tag}
      </a>
    ))
  }
</div>
